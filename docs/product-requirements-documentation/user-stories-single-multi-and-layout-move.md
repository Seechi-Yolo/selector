# 产品系统：用户故事与 PRD 索引

本文是 **选取会话产品系统** 的入口：先锁定 **用户故事**（价值与故事级验收），再落到 **专项 PRD**（可交付行为与编号决策）。事实口径以根目录 `README.md`、`manifest.json` 为准。

---

## PRD 分层（读文档顺序）

| 文档 | 内容 |
|------|------|
| [`selection-session-panel-shell-layout.md`](./selection-session-panel-shell-layout.md) | **壳体与布局**：贴边条、无展开大面板、悬浮已选列表、磁吸与体量（**L-xx**）。 |
| [`selection-session-interaction-and-state.md`](./selection-session-interaction-and-state.md) | **交互与状态**：页上选取与暂停、说明两层模型、**Enter**、Esc、剪贴板、操作引导状态机（**I-xx**）。 |
| [`selection-session-slash-and-multi-step.md`](./selection-session-slash-and-multi-step.md) | **Slash / 选取会话内样式·动作·参考**（**S-xx**；**尚未在会话 UI 交付**，与下表「实现对照」一致）。 |
| [`tutorial-and-sandbox.md`](./tutorial-and-sandbox.md) | 扩展内教程、沙箱与设计系统顶栏；主路径与素材体例（实现见 `src/pages/help-hub/`、`design-system/`）。 |
| [`m2-click-target-depth.md`](./m2-click-target-depth.md) | 里程碑二挂起：点选命中粒度。 |

---

## 实现对照（产品经理视角，与仓库行为一致）

| 故事 | **当前已满足（M1 可验收）** | **缺口 / 体验债（文档与路线图跟踪）** |
|------|------------------------------|----------------------------------------|
| **一** | 单选、**Enter** 开画布下说明、**复制提示词** 含定位与 **修改说明**；贴边条 + 悬浮已选（**L-02**）不挡主路径。 | 无结构性缺口；窄条上辅句过多时可读性依赖实现微调（**I-24**）。 |
| **二** | 多选形成期、整体→逐项、**I-14** 顺序；键盘与页上角标路径完整。 | 多选时引导信息密度高，用户需自行熟悉 **Enter** / 包络角标优先级（教程已写）。 |
| **三** | **仅依赖用户说明 + 多选元数据**：可用 **Shift / 框选** 同时选中「被移动项」与「参考项」，在 **修改说明** 或 **对当前选取的说明** 中手写空间关系；导出含各节点结构化段落（**I-14**）。 | **选取会话内**尚无「选动作 → 再点参考」一步式流程（**S-03**、**S-04** 未实现）；Slash 仍为拟议。 |
| **四** | **扩展顶栏「设计系统」页**：默认 Volt 精简令牌 + **可复制的提示词片段**；用户将片段 **粘贴进「修改说明」** 后，**复制提示词** 自然带上（与故事一同一管道）。沙箱内有「设计系统速览」入口提示；**选取会话**在未开说明、已有选取时，贴边条辅句含 **「样式：扩展菜单→设计系统」**（单选与多选同句，**I-24**）。 | **选取会话内**尚无「选样式 → 自动写入导出字段」一站式能力（**S-02** 未实现）；与宿主设计库实时同步不在 M1 范围。 |

**结论**：故事一、二与当前实现对齐；故事三、四的 **主价值** 在 M1 可用手动路径满足，**原验收中绑定 Slash 的句子** 与实现不符，已改为上表分层表述。

---

## 故事与 PRD 对应

| 故事 | 核心价值 | 主要 PRD 落点 |
|------|----------|----------------|
| **一** | 单元素修改说明 + 复制提示词 | **I-28**、**I-22**、**I-14**；壳体 **L-02**（已选列表） |
| **二** | 多选整体说明再逐项 + 复制提示词顺序 | **I-12**、**I-13**、**I-14**、**I-17** |
| **三** | 移动与参考位置、结构化上下文 | **I-13** + 多选/说明由用户撰写；**S-03**、**S-04** 为选取会话内增强（未交付） |
| **四** | 设计系统样式进入导出 | **M1**：顶栏 **设计系统** 页片段 → 粘贴 **修改说明** → **I-14**；**S-02** 为会话内增强（未交付） |

---

## 故事一：只改一个页面元素

**角色**：在真实页面上改 UI 或排版的编辑者（或配合 AI 写代码的开发者）。

**场景**：页面上只有一个区块需要调整，不需要多选或整段范围说明。

**目标**：我能**点选该元素**，在说明关状态下按 **Enter** 打开画布下的 **「修改说明」**，写清这一项要怎么改；变更会参与 **复制提示词** 的拼装（含防抖自动写剪贴板与提交时再刷新），以便我把**单目标、单意图**交给下游 AI 或同事。

**验收要点（故事级）**：

- 选取建立后默认不弹输入层；**Enter** 后出现该项下的 **修改说明** 浮层（与 **I-28**、**I-22** 一致）。
- **复制提示词** 中能唯一定位到该元素（选择器、上下文等），并带上该项 **修改说明** 正文。

---

## 故事二：同时对多个已选元素下「修改指令」

**角色**：一次改一批相关控件的编辑者。

**场景**：用 **范围选取** 或 **Shift+点击** 选中多个元素；需要先交代这批选取**共同**要达成什么，再对其中若干项写**各自**的修改说明。

**目标**：多选形成结束后，我能先按 **Enter** 打开 **「对当前选取的说明」**（整体层），写完并提交放行后，再针对焦点项或逐项入口写 **「修改说明」**；最终 **复制提示词** 里**整体块在逐项之前**（与 **I-13**、**I-14** 线性默认一致），以便 AI 同时看到**整批意图**和**各元素差异**。

**验收要点（故事级）**：

- **≥2** 项完成后先处于说明关 **N**，**Enter** 进入整体说明 **O**；整体放行后才进入逐项路径。
- 导出文稿同时包含可选的 **对当前选取的说明** 与各元素的 **修改说明**（无冗余项时可省略对应段落，口径以 **I-14** 为准）。

---

## 故事三：把某个按钮移动到另一容器的「上方」或指定参考位置

**角色**：调整布局或信息架构的产品 / 前端使用者。

**场景**：页面上有一个**待移动的按钮**（或同类控件），以及一个**目标容器**或**可见的参考元素**（例如「放到侧栏容器顶」「相对某标题上方留白对齐」）。

**目标**：通过选取与说明，让 **复制提示词** 里同时带上**被移动控件**与**目标/参考位置**的 DOM 与文案上下文，并在 **修改说明** 或（多选时）**对当前选取的说明** 中写清**空间关系**（例如：移动到某容器上方、与某元素对齐、插入到某列表项之前等），以便接收方 AI 能依据结构化上下文执行布局或代码改动，而不是靠模糊的一句「挪一下」。

**验收要点（故事级）**：

- **（M1 已支持）** 用户至少能**点选或框选**到「被移动项」与「目标容器/参考项」中的相关节点（必要时配合 **Shift+点击** 多选）；说明文字明确写出**相对谁**、**哪一侧/哪一轴**等关系（由用户撰写，产品不强制模板句式）。
- **（M1 已支持）** **复制提示词** 对多选场景包含各选中项的元数据；若使用整体说明，整段意图与逐项说明在文稿中的位置关系符合 **I-14** 默认顺序。
- **（增强项，未默认交付）** 选取会话内 **Slash / 动作「移动」+ 参考点选** 见 [`selection-session-slash-and-multi-step.md`](./selection-session-slash-and-multi-step.md) **S-03**、**S-04**；落地后本条升级为必选验收。

---

## 故事四：选中页上按钮并套用设计系统中的某一按钮样式后复制提示词

**角色**：希望宿主页上的按钮与设计系统（或团队组件库）里已有变体对齐的设计师 / 前端。

**场景**：页面上有一个**真实按钮**（或表现为按钮的控件）需要改版；团队在设计系统文档或 Storybook 等渠道里已经定义了多种**按钮样式**（如 primary / secondary / ghost / danger 等命名与视觉规范）。

**目标**：我能**点选该按钮**，在扩展内（或选取会话相关 UI 中）**浏览并选定**某一设计系统按钮样式；系统将「当前 DOM 上下文 + 所选样式名称 / 令牌 / 规范摘要」一并纳入 **复制提示词**，以便下游 AI 或开发者按同一套设计语言改代码，而不是只在 **修改说明** 里手写「做成主按钮」这类易歧义句子。

**验收要点（故事级）**：

- **（M1 已支持）** 选取目标按钮后，用户有一条清晰路径完成「**到扩展菜单打开顶栏 →「设计系统」→ 复制样式片段 → 粘贴进「修改说明」（可选）→ 复制提示词**」；片段正文进入 **修改说明** 后，导出中具备**可复述**的样式语义（与故事一同一拼装管道）。
- **（M1 已支持）** **复制提示词** 须包含该按钮的定位与上下文（与故事一一致）；若 **修改说明** 中含从设计系统页粘贴的片段，与 DOM 元数据**可合并理解**。
- **（增强项，未默认交付）** 在**选取会话贴边条内**一键选样式并写入独立导出字段（**S-02**）落地后，本条补充「无需先打开顶栏页」的验收。

---

## 与教程素材的提示

若教程或沙箱要演示上述路径，步骤与界面分区须与 [`tutorial-and-sandbox.md`](./tutorial-and-sandbox.md) §3 主路径及 **壳体 / 交互** 两篇 PRD 用语一致后再录制素材。故事四的 **M1** 演示须包含：**扩展菜单 → 教程与沙箱 → 设计系统** 与回到页内 **修改说明** 的衔接。
