export const NS = "ai-editor";
export const AI_ID = "data-ai-id";

/** 沙箱等页面上内嵌主面板的挂载容器；与 `.${NS}-root` 一并视为扩展 UI，不参与选取、不分配 data-ai-id */
export const EMBEDDED_EDITOR_HOST_CLASS = "selector-embedded-editor-host";
